<Window x:Class="TotalCommander.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TotalCommander"
        xmlns:local1="clr-namespace:TotalCommander.ValueConverters"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="720" Width="1280">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local1:FileNameExtensionRemovalConverter x:Key="ExtensionConverter"/>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>
                <MenuItem Header="_Close"/>
                <MenuItem Header="_Save"/>
            </MenuItem>
            <MenuItem Header="_Edit"/>
            <MenuItem Header="_Help"/>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" DataContext="{Binding LeftGridViewModel}">
                <StackPanel DockPanel.Dock="Top"
                            Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding Drives}"
                              DisplayMemberPath="Name"
                              SelectedIndex="0"/>
                    <TextBlock Text="{Binding CurrentPath}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Files}" 
                          DockPanel.Dock="Bottom" 
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Name, Mode=OneWay}"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Length}"/>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding ChangePathCommand}"
                                      CommandParameter="{Binding SelectedItem}"/>
                    </DataGrid.InputBindings>
                </DataGrid>
            </DockPanel>
            <DockPanel Grid.Column="2" DataContext="{Binding RightGridViewModel}">
                <StackPanel DockPanel.Dock="Top"
                            Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding Drives}"
                              DisplayMemberPath="Name"
                              SelectedIndex="0"
                              SelectedItem="{Binding SelectedDrive}"/>
                    <TextBlock Text="{Binding CurrentPath}"/>                     
                </StackPanel>
                <DataGrid Name="rightDataGrid"
                    ItemsSource="{Binding Files}" 
                          DockPanel.Dock="Bottom" 
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Name, Mode=OneWay, Converter={StaticResource ExtensionConverter}}"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Length, Mode=OneWay}"/>
                        <DataGridTextColumn Header="Extension" Binding="{Binding Extension, Mode=OneWay}"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding LastWriteTime, Mode=OneWay, StringFormat='dd/MM/yyyy'}"/>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding ChangePathCommand}"
                                      CommandParameter="{Binding Path=SelectedItem, ElementName=rightDataGrid}"/>
                    </DataGrid.InputBindings>
                </DataGrid>
            </DockPanel>
        </Grid>
    </DockPanel>
    
</Window>
