<Window x:Class="TotalCommander.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TotalCommander"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="720" Width="1280">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>
                <MenuItem Header="_Close"/>
                <MenuItem Header="_Save"/>
            </MenuItem>
            <MenuItem Header="_Edit"/>
            <MenuItem Header="_Help"/>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" DataContext="{Binding LeftGridViewModel}">
                <TextBlock Text="{Binding Path}" DockPanel.Dock="Top"/>
                <DataGrid ItemsSource="{Binding Files}" 
                          DockPanel.Dock="Bottom" 
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Name, Mode=OneWay}"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Length}"/>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding ChangePathCommand}"
                                      CommandParameter="{Binding SelectedItem}"/>
                    </DataGrid.InputBindings>
                </DataGrid>                
            </DockPanel>
            <StackPanel Grid.Column="1"/>
            <DockPanel Grid.Column="2" DataContext="{Binding RightGridViewModel}">
                <Grid DockPanel.Dock="Top">
                    <ComboBox ItemsSource="{Binding Path=Drives}"
                              Height="100"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Name"
                              IsEditable="False"/>
                    <TextBlock Text="{Binding Path}"/>                    
                </Grid>
                <!--<DataGrid ItemsSource="{Binding Files}" 
                          DockPanel.Dock="Bottom" 
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Name, Mode=OneWay}"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Length}"/>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding ChangePathCommand}"
                                      CommandParameter="{Binding SelectedItem}"/>
                    </DataGrid.InputBindings>
                </DataGrid>-->
            </DockPanel>
        </Grid>
    </DockPanel>
    
</Window>
